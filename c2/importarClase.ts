// clase_b.ts
import { MiClase } from './exportarClase';

// Creamos una instancia de la clase MiClase
const instancia = new MiClase("Juan");

// Llamamos al método saludar
instancia.saludar(); // Esto imprimirá "Hola, soy Juan"
